import { formatearFecha, formatDia, formatMes } from './FormatearFecha';
import useFecha from '../hooks/UseFecha';
import { useState, useEffect } from 'react';

//Declaramos el calendario y los dias internos
const calendario = ['enero', 'febrero', 'marzo', 'abril', 'mayo', 'junio', 'julio', 'agosto', 'septiembre', 'octubre', 'noviembre', 'diciembre'];

const FiltroComandas = () => {
    return (
        <div>

        </div>
    )
}

export default FiltroComandas
